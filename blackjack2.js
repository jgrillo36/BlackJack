let cardDeck =[];
let suits = ["C", "D", "H", "S"];
let suitMap = {
    C: "clubs",
    D: "diamonds",
    H: "hearts",
    S: "spades"
}
function createCardObj(points, suit){
    // let image url
    let card ={
        points: points,
        suit: suit
    }
        if(points > 1 && points <=10){
            
        }
}

function buildDeck(){

    suits.forEach(suit=>{
        for(let points = 1; points <=13; points++){
            cardDeck.push()

        } //end of for

    }) //end of foreach
}